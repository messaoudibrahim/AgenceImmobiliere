{% extends 'base.html.twig' %}

{% block title %}Edition{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Edition d'une Option</h1>

        {{ include('admin/option/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="row mt-2">
            <div class="col-md-4">
            <a href="{{ path('option_index') }}" class="btn btn-primary">Reviens a la liste</a>
            </div>
            <div class="col-md-4">
                {{ include('admin/option/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
