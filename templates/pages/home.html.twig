{% extends "base.html.twig" %}
{% block body %}
    <div class="jumbotron text-center">
        <h1>Votre Agence</h1>
        <p>Le Lorem Ipsum est simplement du faux texte employé dans la composition et la mise en page avant
            impression. Le Lorem Ipsum est le faux texte standard deapté à la
            </p>
    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="flex row">
            {% for property in properties %}
            <div class="col-3">
                <div class="card">
                    <div class="card-body">
                        <h3 class="card-title">
                            <a href="{{ path('property.show', {id: property.id, slug: property.slug})}}"> {{ property.title }}</a>
                        </h3>
                        <p class="card-text"> {{ property.city}} ({{ property.postalCode }})</p>
                        <h3><div class="text-primary">{{ property.formatPrice }} Euros</div></h3>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}